function Hd = untitled
%UNTITLED Returns a discrete-time filter object.

% MATLAB Code
% Generated by MATLAB(R) 9.11 and DSP System Toolbox 9.13.
% Generated on: 09-May-2025 13:55:47

% Butterworth Bandpass filter designed using FDESIGN.BANDPASS.

% All frequency values are in Hz.
Fs = 8000;  % Sampling Frequency

Fstop1 = 150;         % First Stopband Frequency
Fpass1 = 300;         % First Passband Frequency
Fpass2 = 3400;        % Second Passband Frequency
Fstop2 = 3550;        % Second Stopband Frequency
Astop1 = 10;          % First Stopband Attenuation (dB)
Apass  = 1;           % Passband Ripple (dB)
Astop2 = 10;          % Second Stopband Attenuation (dB)
match  = 'stopband';  % Band to match exactly

% Construct an FDESIGN object and call its BUTTER method.
h  = fdesign.bandpass(Fstop1, Fpass1, Fpass2, Fstop2, Astop1, Apass, ...
                      Astop2, Fs);
Hd = design(h, 'butter', 'MatchExactly', match);
% Set the arithmetic property.
set(Hd, 'Arithmetic', 'fixed', ...
    'CoeffWordLength', 12, ...
    'CoeffAutoScale', true, ...
    'ProductMode', 'KeepLSB', ...
    'ProductWordLength', 24, ...
    'AccumMode', 'KeepMSB', ...
    'AccumWordLength', 40, ...
    'StateWordLength', 12, ...
    'StateFracLength', 12, ...
    'SectioninputWordLength', 12, ...
    'SectioninputAutoScale', true, ...
    'SectionoutputWordLength', 12, ...
    'SectionoutputAutoScale', true, ...
    'InputWordLength', 12, ...
    'inputFracLength', 0, ...
    'OutputWordLength', 12, ...
    'OutputMode', 'AvoidOverflow', ...
    'signed',        true, ...
    'RoundMode',     'convergent', ...
    'OverflowMode',  'Saturate', ...
    'CastBeforeSum', true);



% [EOF]
